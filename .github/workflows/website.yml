name: Deploy Website

on:
  push:
    branches:
      - main
    paths:
      - website/**

jobs:
  deploy_production:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Check if this works
        run: |-
          echo "Hello World"
